spring.datasource.url=${DATASOURCE_URL:jdbc:oracle:thin:@//localhost:1521/assemblyvote}
spring.datasource.username=${DATASOURCE_USER:appuser}
spring.datasource.password=${DATASOURCE_PASSWORD:passwd}

spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.password=${REDIS_PASSWORD}
spring.data.redis.port=${REDIS_PORT:6379}

application.session-duration-sec.default=${SESSION_DURATION:60}
scheduler.voting.close-delay=${SCHEDULER_VOTE:10000}

feign.client.validate-member.url=${VALIDATE_MEMBER_URI:https://validate-member-deploy-render.onrender.com}